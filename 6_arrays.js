const colors = ['red', 'green', 'blue']

colors.length // 3

colors[1] // 'green'

colors.push('purple') // colors = ['red', 'green', 'blue', 'purple']
colors.pop() // 'purple'
colors.pop() // 'blue'
